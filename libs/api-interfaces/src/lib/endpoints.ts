export enum API_ENDPOINTS {
  skills = 'skills',
  experiences = 'experiences',
  certifications = 'certifications',
  profiles = 'profiles',
  blogs = 'blogs',
  photos = 'photos',
  files = 'files',
  resume = 'resume',
  auth = 'auth',
  all = 'all',
  me = 'me',
  download = 'download',
}

export enum AUTH_ENDPOINTS {
  login = 'login',
  check = 'check',
}

export enum PHOTOS_ENDPOINTS {
  image = 'image',
}
