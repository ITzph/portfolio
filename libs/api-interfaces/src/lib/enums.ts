export enum TIME_ZONE {
  DEFAULT = 'en',
  LOCAL = 'sg',
}

export enum FileCategory {
  DOCUMENT = 'DOCUMENT',
  OTHER = 'OTHER',
}

export enum ImageCategory {
  MEME = 'MEME',
  QUOTE = 'QUOTE',
}
